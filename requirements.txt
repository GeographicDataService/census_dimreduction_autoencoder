# Mirrors the [project].dependencies list in pyproject.toml.
# uv sync (via uv.lock) is the authoritative environment; this file is the pip fallback.
#
# torch/torchvision/torchaudio resolve from PyPI here — CUDA-enabled, but currently cu130.
# For the CUDA 12.8 build used in the paper, install them from the PyTorch index BEFORE
# installing this file — see README.
# (An --extra-index-url line here is not viable: that index also mirrors tqdm at an older
# version, and uv's default index strategy then fails to resolve.)

# data / geo
geopandas>=1.0
pyarrow>=17.0
pandas>=2.2
numpy>=2.0
scipy>=1.13
scikit-learn>=1.5
joblib>=1.4

# spatial analysis (PySAL)
esda>=2.6
libpysal>=4.12

# dimensionality reduction / viz
umap-learn>=0.5.6
numba>=0.61
llvmlite>=0.44
matplotlib>=3.9
seaborn>=0.13
tqdm>=4.67
plotly>=6.8.0
kaleido>=1.3.0

# deep learning (CUDA 12.8)
torch>=2.7
torchvision
torchaudio

# notebook runtime
ipykernel>=6.29
jupyterlab>=4.2

# autoencoder package (vendored copy in this repo)
./torchgeodemo
